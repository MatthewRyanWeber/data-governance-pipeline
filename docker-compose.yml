services:
  pipeline-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PIPELINE_API_KEYS=${PIPELINE_API_KEYS}

  test:
    build:
      context: .
      target: test
    profiles:
      - test
