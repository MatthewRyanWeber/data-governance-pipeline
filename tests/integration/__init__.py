"""Live-engine integration tests — real databases, emulators, containers.

One module per engine family.  Everything here carries
@pytest.mark.integration and runs in the dedicated CI integration job,
not the unit job.
"""
